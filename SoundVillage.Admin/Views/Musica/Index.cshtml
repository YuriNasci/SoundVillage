@model IEnumerable<SoundVillage.Application.Admin.Dto.MusicaDto>
<div>
    <a class="btn btn-primary" role="button" href="/Musica/Criar">Criar Música</a>
</div>
<div>
    <table class="table table hover">
        <thead>
            <tr>
                <td>Nome</td>
                <td>Artista</td>
                <td>Album</td>
                <td>Duração</td>
                <td>Ações</td>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Nome</td>
                    <td>@item.NomeArtista</td>
                    <td>@item.NomeAlbum</td>
                    <td>@item.Duracao</td>
                    <td>
                        <div class="btn-group" role="group">
                            <a asp-action="Editar" asp-route-id="@item.Id" class="btn btn-primary">Editar</a>
                            <a asp-action="Excluir" asp-route-id="@item.Id" class="btn btn-danger">Excluir</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
