@model IEnumerable<SoundVillage.Application.Admin.Dto.UsuarioAdminDto>
<div>
    <a class="btn btn-primary" role="button" href="/User/Criar">Criar Usuário</a>
</div>
<div>
    <table class="table table hover">
        <thead>
            <tr>
                <td>Id</td>
                <td>Nome</td>
                <td>Email</td>
                <td>Perfil</td>
                <td>Ações</td>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Nome</td>
                    <td>@item.Email</td>
                    <td>@(item.Perfil == 1 ? "Full": "Normal")</td>
                    <td>
                        <div class="btn-group" role="group">
                            <a asp-action="Editar" asp-route-id="@item.Id" class="btn btn-primary">Editar</a>
                            <a asp-action="Excluir" asp-route-id="@item.Id" class="btn btn-danger">Excluir</a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>